# Named Sub-Index: `stt`

## Entries

- 2026-08-26 WIB — Sisyphus: Phase 3 accepted. The manually verified six-recording ground-truth manifest was benchmarked cache-only with `large-v3-turbo` CPU INT8: exact rate 1.0, WER 0.0, CER 0.0; 48.0 s audio required 56.84 s processing (RTF 1.18). Select `large-v3-turbo` CPU INT8 as the current STT baseline. It is functional but not final latency tuning; do not download `large-v3` or `small` unless a later benchmark establishes a need. Phase 4 is `NEXT`; no LLM request has been made.
- 2026-08-26 WIB — Sisyphus: First raw CPU INT8 `large-v3-turbo` benchmark completed with six 8 s Indonesian/code-switch samples from the verified local cache: RTF 1.064 (51.09 s processing / 48.0 s audio), WER 0.437, CER 0.186, exact rate 0.0. These text metrics are provisional because manifest references may differ from the speaker's actual wording; `ROCm` was intentionally omitted from the recording and must not be treated as a model omission. Review errors only after comparing each raw transcript to the audible recording. Phase 3 remains open; compare `large-v3` with the same audio before considering `small`. Phase 4 remains blocked.
- 2026-08-26 WIB — Sisyphus-Junior: Phase 3 automated Faster-Whisper STT vertical slice implemented and left uncommitted. Added `faster-whisper 1.2.1` with generic `ctranslate2 4.8.1`; `uv tree` shows no CUDA/ROCm/HIP/NVIDIA/Triton packages. Contracts accept only contiguous mono 16 kHz float32 utterances, `SttConfig` allowlists `large-v3-turbo`, `large-v3`, `small`, CPU, INT8, external cache `~/.cache/holo-companion/models`, `local_files_only`, `cpu_threads`, and `num_workers`. Provider tests inject a fake loader/clock, so no model loads/downloads occur. CLI now has `stt-wav`, `stt-benchmark`, and `vad-test --transcribe`; VAD transcription uses the existing single frame stream plus collector and no duplicate capture. Model-free suite passed 65 tests; compileall/help/diff/tree gates passed. LSP daemon was unreachable after retries, so LSP diagnostics could not run. Manual real model benchmark remains pending; ROADMAP Phase 3 stays `NEXT` and Phase 4 stays blocked.
