# Named Sub-Index: `tts`

## Entries

- 2026-09-08 WIB — Hephaestus: Phase 5 COMPLETE by explicit user acceptance. ElevenLabs real streaming smoke tests passed; Holo Soft v1 voice identity, playful delivery, concerned delivery, and emotional distinction accepted. Development configuration: `eleven_v3_conversational`, stability 0.40, Indonesian Text-to-Dialogue WebSocket, PCM 24 kHz mono, accepted EmotionMapper. User-reported concerned benchmark: TTFA 0.262 s, synthesis 0.743 s, audio 3.120 s, RTF 0.238, 149760 bytes, 6 chunks. F5-TTS Indonesian V2 remains frozen as the local personal/research fallback; preserve all environments, caches, and tests. This supersedes earlier candidate/blocked-status entries. Phase 6 is next but NOT STARTED; no RVC. See ROADMAP.md for acceptance evidence.
- 2026-09-08 WIB — Sisyphus: Added safe staged ElevenLabs smoke diagnostics. `TtsError` may carry only allowlisted `TtsProviderDiagnostic`: stage, HTTP status, WebSocket close code, controlled error/error class, sanitized provider message, and validated model. ElevenLabs maps config, handshake, registration, input send, provider frame, receive, timeout, socket close, and audio decode without copying secrets, headers, raw frames, URLs, request objects, or exception strings. `tts-smoke` prints only populated labeled diagnostics and redacts generic provider failures. Mock guards cover API-key safety, sanitized provider error, handshake status, close path, timeout, malformed frame/base64, and diagnostic CLI output. Final `uv run pytest` 127 passed; compileall, lock, and diff checks passed. No real request; Phase 6 remains blocked.
- 2026-09-08 WIB — Sisyphus: Added Phase 5 developer-only `holo-companion tts-smoke` CLI. It loads the existing ElevenLabs config/provider path from shell-exported env, makes one provider stream, stages a same-directory temporary PCM_16 mono WAV, atomically publishes only after a completed terminal result, emits safe labeled TTFA/latency/duration/RTF/bytes/chunk/provider/model metrics, and redacts provider error details at the CLI boundary. `tests/test_tts_smoke_cli.py` uses only fake providers and verifies WAV validity, style transport, missing config, empty/provider failures, output directory, metrics, atomic cleanup, and secret-safe output. Final `uv run pytest` 126 passed; compileall, lock check, diff check, and CLI help passed. No paid request; Phase 6 remains blocked.
- 2026-09-08 WIB — Sisyphus-Junior: Replaced ElevenLabs TTS HTTP transport with official Text to Dialogue WebSocket transport. `src/holo_companion/tts/elevenlabs.py` now uses an injected async WebSocket connector/socket protocol, sends `wss://.../v1/text-to-dialogue/stream-input?model_id=eleven_v3_conversational&output_format=pcm_24000&language_code=id` with `xi-api-key`, registers exactly one configured voice, sends one `inputs` frame with rendered text plus configured voice ID and `flush: true`, decodes official base64 audio frames incrementally with PCM16 carry, handles `is_final_audio_for_turn`, `is_final`, provider error payloads, timeout, cancellation, and socket cleanup. Provider-private frame/socket helpers live in `src/holo_companion/tts/_elevenlabs_ws.py`. Added direct dependency `websockets>=15.0`; uv resolved `websockets==17.1`. Tests use only in-memory fake sockets; no live ElevenLabs calls. Verification: red fake WebSocket tests failed against old `client=` HTTP provider; focused `uv run pytest tests/test_elevenlabs_tts.py` 14 passed; full `uv run pytest` 120 passed; `uv run python -m compileall src tests` passed; `uv lock --check` passed; TTS source grep found only text-to-dialogue path and no TTS `httpx`/REST references. LSP daemon unavailable at `/home/arjuna/.omo/lsp-daemon/v0.1.0/daemon.sock`; `basedpyright` executable not installed. LOC: `elevenlabs.py` 199, `_elevenlabs_ws.py` 80, `test_elevenlabs_tts.py` 242 warning band.
- 2026-09-08 WIB — Sisyphus: Added mock-only ElevenLabs Phase 5 adapter. `src/holo_companion/tts/elevenlabs.py` streams documented `POST /v1/text-to-speech/{voice_id}/stream?output_format=pcm_24000` with `xi-api-key`, exact `eleven_v3_conversational`, raw PCM16 to 24 kHz mono float32 conversion with odd-byte carry, safe typed errors, cancellation terminal result after first chunk, and provider/model/TTFA/latency/audio-duration/RTF/chunk/byte metrics. Deterministic `EmotionMapper` prepends V3 audio tags while preserving original dialogue words: neutral none; happy `[happy] [softly]`; playful `[mischievously] [whispers]`; relaxed `[softly]`; concerned `[reassuring] [softly]`; sad `[sad] [sighs]`; angry `[frustrated]`; surprised `[surprised]`; shy `[whispers] [sighs]` at intensity >=0.5, otherwise base tag only. `src/holo_companion/tts/config.py` consumes only `ELEVENLABS_API_KEY`, `ELEVENLABS_VOICE_ID`, `ELEVENLABS_MODEL_ID`; no live request was made. Final verification: `uv run pytest` 117 passed; compileall and `uv lock --check` passed; LSP daemon unavailable at `/home/arjuna/.omo/lsp-daemon/v0.1.0/daemon.sock`. Phase 6 remains blocked.
- 2026-09-07 WIB — Sisyphus: Human listening rejected `aisyahsyihab/voxcpm-lora-indonesian-female-v2`: female but insufficient naturalness/expression, poor Indonesian and Indo-English. Do not tune rejected Chatterbox/VoxCPM candidates further. Next personal/research-only candidate is `Eempostor/F5-TTS-INDO-FINETUNE-V2` (CC-BY-NC-4.0), benchmarked only with one clean licensed/consented Indonesian female reference; never use identifiable third-party voices. Preserve all existing environments/artifacts, use an isolated F5 environment and `/mnt/data/models/holo-companion/tts/f5-indonesian/`, and keep Phase 6 blocked.
- 2026-09-07 WIB — Sisyphus: Human RTX 5050 benchmark rejected `grandhigh/Chatterbox-TTS-Indonesian` as Holo default: male/non-native Indonesian timbre, flat delivery, insufficient naturalness. Measured torch 2.9.0+cu128 on RTX 5050 sm_120: peak/reserved VRAM 4076/4106 MB, 1.993 s for 3.28 s audio, RTF 0.607, no OOM. Do not pitch-shift or use third-party cloning as default. Next benchmark candidate is `aisyahsyihab/voxcpm-lora-indonesian-female-v2` over `openbmb/VoxCPM1.5`; no download or install approved yet. Keep cache at `/mnt/data/models/holo-companion/tts/voxcpm-indonesian-female/`, retain Chatterbox artifacts, preserve existing environments, and keep Phase 6 blocked.
- 2026-08-27 WIB — Sisyphus: Phase 5 portability rule updated. Keep one provider-neutral core and one shared model cache at `/mnt/data/models/holo-companion/tts/`; `.venv-rocm` and `.venv-cuda` may differ by host but must not duplicate weights. VoxCPM2 is the first AMD experimental benchmark only, not a default selection. A final default requires the same quality/latency benchmark on RX 9060 XT 16 GB ROCm and RTX 5050 8 GB CUDA. Classify candidates as DEFAULT PORTABLE, HIGH-QUALITY OPTIONAL, or REJECTED; include Indonesian/code-switch quality, TTFA, RTF, peak VRAM/RAM, both-host compatibility, 8 GB fit, and fallback behavior. Phase 6 remains blocked until this strategy is decided.
- 2026-08-27 WIB — Sisyphus-Junior: Added first automated Phase 5 TTS foundation only. New `src/holo_companion/tts/base.py` defines provider-neutral `TTSProvider`, `SynthesisRequest`, `StyleHints`, mono native-rate float32 `TTSAudioChunk`, `SynthesisMetrics`, terminal `SynthesisResult`, typed `TtsError`, and pure aggregate duration/RTF helpers. New `tests/test_tts_base.py` uses deterministic in-memory fake streaming only; no provider package, model, config, cache, download, playback, runtime events, response planner, Phase 6, roadmap completion, or commit. Verification: red test first failed on missing `holo_companion.tts`; focused TTS pytest 15 passed; full pytest 106 passed; compileall passed; `uv lock --check` passed; LOC `base.py` 127, `tts/__init__.py` 30, `tests/test_tts_base.py` 149. LSP diagnostics could not run because `/home/arjuna/.omo/lsp-daemon/v0.1.0/daemon.sock` was unreachable. QwenTTS remains a research recommendation only; before any Qwen/Fish/GPT-SoVITS provider work, require explicit user action to choose cache/checkpoint location and approve any large model pre-download after exact storage/VRAM requirements are known. Phase 5 remains `NEXT`; Phase 6 remains blocked.
