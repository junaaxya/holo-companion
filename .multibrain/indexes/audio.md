# Named Sub-Index: `audio`

## Entries

- 2026-08-21 WIB — Sisyphus: User confirmed `recordings/mic-test-final.wav` is clear and free of audible distortion at 40% PipeWire source gain. Phase 1 is complete: device enumeration, default-route recording, explicit 16 kHz mono float32-to-PCM_16 conversion, listening inspection, and basic clipping/volume/noise/echo check are validated. The accepted 10 s recording had 68 clipping samples of 160,000 (0.043%) and RMS 0.096; this is acceptable for the current phone-headset microphone. Phase 2 VAD is now `NEXT`, but no Phase 2 code was started.
- 2026-08-21 WIB — Sisyphus: After reducing PipeWire source 42 gain to 40%, a 10 s manual capture (`recordings/mic-test-final.wav`) reported 160,000 frames, RMS 0.09649109798550516, peak 1.2197431325912476, and only 68 clipping samples (0.043%). This is a large improvement from the original 46.2% clipping. Listening confirmation remains required before Phase 1 quality-gate completion; Phase 2 stays blocked.
- 2026-08-21 WIB — Sisyphus: User performed the first real `auto` recording to `recordings/mic-test.wav`. Capture produced 80,000 frames / 5.0 s, but diagnostics reported peak 1.2217826843261719, RMS 0.9925009824596044, and 36,993 clipping samples (`clipping: true`). The default route resolved to PipeWire/PulseAudio `default` at observed index 9. Phase 1 quality gate remains blocked pending listening result and host source/gain inspection; do not advance Phase 2.
- 2026-08-21 00:00 WIB — Sisyphus-Junior: Fixed Phase 1 SoundDevice `auto` runtime defaults so int or name defaults resolve through `sounddevice.query_devices(default, kind)` and return observed `DeviceInfo`; adapter now converts expected query/settings/record `ValueError` and `PortAudioError` into concise `AudioCliError`. Added hardware-free regression tests for named input/output defaults, missing/negative defaults, CLI exit-2/no-traceback, and adapter error conversion. Verified `uv run --locked pytest`, `compileall`, and `python -m holo_companion devices`; LSP daemon remained unreachable. Physical mic/speaker acoustic validation remains user-only.
- 2026-08-21 00:00 WIB — Sisyphus-Junior: Corrected Phase 1 review gaps: `devices` now reports separate runtime PortAudio `auto` resolutions for input and output, validates the observed default output has output channels, keeps output observation-only with no playback/capture API, and split the oversized audio tests into focused modules under 250 pure LOC. Verified `uv run --locked pytest`, `compileall`, `python -m holo_companion --help`, `python -m holo_companion devices`, and fallback pure LOC counts; no-excuse script absent; LSP daemon unreachable/timeouts persisted. Real microphone/speaker quality validation remains user-only.
- 2026-08-21 00:00 WIB — Sisyphus-Junior: Added minimal Phase 1 Audio I/O CLI slice under `src/holo_companion/`: PortAudio device enumeration, runtime `auto` default resolution, invocation-scoped input name query, mono 16 kHz float32 capture, WAV PCM_16 writing, deterministic diagnostics, and hardware-free pytest fakes. Verified `uv run --locked pytest`, `compileall`, no-excuse checker, `python -m holo_companion --help`, `devices`, and non-recording validation smokes. LSP MCP daemon was unreachable. Real microphone recording/listening remains user-only.
