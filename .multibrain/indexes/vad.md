# Named Sub-Index: `vad`

## Entries

- 2026-08-26 WIB — Sisyphus: Dependency audit retained `torch` and `torchaudio`. `silero-vad 6.2.0` declares both as mandatory runtime requirements and imports both at package load; the project adapter explicitly uses `torch.from_numpy` for the required NumPy-to-CPU-tensor boundary, while Silero's ONNX wrapper keeps tensor-based context/state around ONNX Runtime. Removing either would require bypassing official package metadata or changing the working provider. Both remain pinned to uv's explicit official CPU index; no GPU packages are present.
- 2026-08-25 WIB — Sisyphus: Phase 2 physical quality gate passed after the probability-based transient fix. With threshold 0.6, min speech 500 ms, and min silence 700 ms, the 30 s headset test emitted exactly two complete turns, rejected four short noise/transient candidates, preserved the mid-sentence pause, had no queue overrun, averaged 0.233 ms provider processing per frame, and measured 704 ms end latency. These tested values are now CLI defaults. Phase 2 is `COMPLETE`; Phase 3 STT is `NEXT`, but no Phase 3 code was started.
- 2026-08-25 WIB — Sisyphus: Physical tuning at threshold 0.6 / min speech 500 ms / min silence 700 ms exposed a real transient-filter bug: a 192 ms `eh` emitted a public turn because `VADIterator` hid the speech probability and delayed its end event until after silence debounce. Replaced iterator event mapping with packaged ONNX model probability per canonical frame; generic `TurnDetector` now owns min-speech and min-silence timers. Added failing-first regression `test_detector_rejects_192ms_transient_when_minimum_is_500ms`; targeted tests and full suite pass (47 tests), packaged model probability smoke passes, CPU-only dependency tree remains free of GPU packages. Retest with the same physical pattern is pending.
- 2026-08-25 WIB — Sisyphus: Phase 2 automated slice corrected and verified. `torch`/`torchaudio` are pinned through uv's explicit official `pytorch-cpu` index; resolved runtime is torch 2.13.0+cpu with CUDA false, ONNX CPU available, and no NVIDIA/CUDA/ROCm/Triton packages. Public events moved to `runtime/events.py`; `TurnDetector` delays `SpeechStarted` until 250 ms, rejects shorter transients, pairs ends after Silero's 500 ms silence debounce, and reports turn/timing diagnostics. Silero adapter explicitly converts contiguous NumPy float32 frames to CPU tensors and handles real iterator `None` results. Packaged ONNX model smoke passed; full suite passed 49 tests. Physical `vad-test` remains pending; ROADMAP Phase 2 remains `NEXT`, Phase 3 blocked.
- 2026-08-21 WIB — Sisyphus-Junior: Implemented the approved smallest Phase 2 CPU-only streaming VAD slice without changing ROADMAP status. Added canonical 512-sample/32 ms 16 kHz mono float32 `AudioFrame` conversion, `SoundDeviceBackend.frame_stream()` over `sounddevice.InputStream` with queue size 8 and `AudioCliError` overrun/errors, provider-independent `SpeechStarted`/`SpeechEnded` events, `TurnDetector` min-speech filtering and end-latency diagnostics, isolated `vad/silero.py` using `load_silero_vad(onnx=True)` plus `VADIterator`, and bounded `vad-test` CLI JSON diagnostics. Added hardware-free tests for conversion, stream cleanup/overrun, detector transitions, Silero mapping/reset, and CLI JSON/parse failure. Local non-uv verification passed with 47 pytest tests and compileall; required final uv/LSP/git checks still need completion in this session. Physical microphone VAD test intentionally not run.
