# Named Sub-Index: `runtime`

## Entries

- 2026-09-10 WIB — Sisyphus: Finalized ElevenLabs HTTP streaming as default transport (`POST /v1/text-to-dialogue/stream?output_format=pcm_24000`) and validated end-to-end assistant reply playback completion. Cleaned up temporary noisy WebSocket stdout logging. Clarified and restored test suite coverage (now 214 tests): reinstated EmotionMapper and config tests, preserved HTTP streaming regression tests (multiple chunks, partial byte carry, cancellation stream closure, status error, timeout error), and added orchestrator test proving normal multi-turn assistant replies play to full completion without premature cutoff. Full offline verification: 214 pytest passed, compileall, uv lock --check, git diff --check passed; no live/cloud call and no commit.
- 2026-09-10 WIB — Sisyphus: Replaced ElevenLabs v3 Text-to-Dialogue WebSocket transport with persistent HTTP streaming (`POST /v1/text-to-dialogue/stream?output_format=pcm_24000`) as the default runtime transport. Built `DefaultHttpConnector` backed by `httpx.AsyncClient` with connection pooling, incremental response byte streaming, immediate PCM chunk emission, and prompt cancellation. Preserved provider interfaces, metrics, emotion audio tags, and safe diagnostics. Removed WebSocket stalls and premature truncation. Added unit tests for multi-chunk streaming, partial-byte carry, cancellation stream closure, status error mapping, and timeout mapping. Full offline verification: 208 pytest passed, compileall, uv lock --check, git diff --check passed.

- 2026-09-10 WIB — Sisyphus: Fixed ElevenLabs Text-to-Dialogue WebSocket stream truncation. Recv loop now terminates solely upon receiving `is_final: true` (or socket close/cancellation), rather than prematurely exiting or dropping chunks on `is_final_audio_for_turn: true`. All audio packets received prior to `is_final` are decoded and yielded. Added lightweight lifecycle event diagnostics: `WS send: voices`, `WS send: inputs`, `WS send: close_socket`, `WS recv: audio <N bytes>`, `WS recv: is_final_audio_for_turn`, `WS recv: is_final`, `WS recv: socket_closed` without logging secrets or audio payloads. Added regression test verifying reception of audio chunks continuing after `is_final_audio_for_turn` until final completion. Full offline verification: 219 pytest passed, compileall, uv lock --check, git diff --check passed.

- 2026-09-10 WIB — Sisyphus: Added opt-in `HOLO_DEBUG_TTS_AUDIO=1` debug mode to isolate startup TTS/playback audio truncation without changing normal playback behavior. When enabled, startup greeting accumulates every PCM chunk from ElevenLabs and saves to `/tmp/holo-provider.wav`, and accumulates all PCM actually consumed by the audio output callback and saves to `/tmp/holo-playback.wav`. Startup diagnostics explicitly print: `provider audio chunk count`, `provider total samples`, `provider total duration`, `provider stream completed`, `playback queued samples`, `playback consumed samples`, `playback drain completed`, and `stop/cancel/interruption reason`. Added unit test verifying file dumps and metrics. Full offline verification: 218 pytest passed, compileall, uv lock --check, git diff --check passed.

- 2026-09-10 WIB — Sisyphus: Fixed startup greeting audio truncation and added comprehensive startup diagnostics. Audited startup greeting lifecycle to verify why playback stopped prematurely after punctuation. The full greeting text is submitted to ElevenLabs as a single complete utterance; punctuation does not terminate greeting early. Updated `SoundDevicePlaybackSink.wait_until_drained()` to poll until both sample queue and chunk buffer reach zero, followed by a short hardware buffer drain delay so PortAudio DAC fully outputs the final chunk. Added startup metrics (`startup original text`, `startup text after EmotionMapper`, `exact text submitted to ElevenLabs`, `provider audio chunk count`, `total PCM samples received`, `PCM duration received`, `PCM samples queued to playback`, `provider stream completed normally`, `playback drain completed`, `startup cancellation/stop reason`). Added regression test verifying full multi-chunk PCM consumption for the exact phrase `Eh, Master... udah balik ya? Aku... kangen pengen ngobrol, hehe.`. Full offline verification: 217 pytest passed, compileall, uv lock --check, git diff --check passed.

- 2026-09-10 WIB — Sisyphus: Fixed startup greeting emotional delivery for ElevenLabs v3 dialogue model. The curated greeting texts now embed conservative delivery cues (`[shyly]`, `[softly]`, and subtle hesitation/stuttering like `H-halo, Master...`) directly, ensuring expressive delivery without affecting regular neutral turns. Updated `EmotionMapper.map_text` to preserve existing bracketed audio tags. Spoken text normalizer was audited and verified to preserve audio tags. Added `startup TTS text: ...` diagnostic to `live_cli.py`. Added unit tests verifying shy tags in greeting, absence of tags in neutral synthesis, and audio tags surviving text normalization. Full offline verification: 216 pytest passed, compileall, uv lock --check, git diff --check passed.

- 2026-09-10 WIB — Sisyphus: Added startup spoken greeting and simplified Holo conversational persona. Before entering `STATE: LISTENING`, the live CLI selects a curated Indonesian greeting addressing the user as 'Master' with shy style hints (`emotion='shy'`, `intensity=0.5`), synthesizes it directly via the existing TTS provider without calling the LLM, drains playback, and then begins continuous microphone listening. Simplified `PERSONA_PROMPT` in `llm/context.py` to be warm, familiar, playful, slightly shy, calling user 'Master', answering naturally without rigid fragment rules or forced questions, and retaining Indonesian conversational tone with Markdown restrictions. Added unit and async streaming tests for greeting selection and playback drain. Full offline verification: 213 pytest passed, compileall, uv lock --check, git diff --check passed.

- 2026-09-10 WIB — Sisyphus: Fixed spoken-response over-chunking without changing STT, VAD, barge-in, or providers. Added `normalize_spoken_text()` in `spoken_text.py` to strip Markdown headers, list bullets, bold/italic markers, and inline/line list ordinals before TTS synthesis. Updated `IncrementalSpeechSegmenter` to hold tiny fragments such as `1.` or short labels and merge them with the next sentence clause while preserving streaming sentence boundaries. Updated `PERSONA_PROMPT` in `llm/context.py` to default to complete spoken Indonesian sentences without Markdown, headings, or bullet/numbered lists. Retained temporary text diagnostics (`LLM assistant text`, `segment emitted`, `TTS submitted`, `flushed`, `finish reason`). Added regression tests for Markdown stripping, ordinal merging, comma-heavy natural streams, and multi-sentence responses. Full fake-only verification: 206 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no live/cloud test or commit.

- 2026-09-10 WIB — Sisyphus: Added temporary safe per-turn text completeness diagnostics without changing segmentation thresholds. `TurnResult.text_diagnostic` now records full accumulated LLM text, indexed segmenter emissions, indexed text actually submitted to TTS after bounded queue receive, whether nonempty residual flush emitted a tail, and terminal LLM status. `talk` prints those values for a completed turn. Audit confirms `IncrementalSpeechSegmenter.flush()` always emits a nonblank residual, AnyIO queue sends block under backpressure instead of dropping text, and only existing cancellation/stale ownership guards discard work. Regression coverage proves no-punctuation tail flush, comma-heavy text, multi-sentence streaming order, emitted-to-TTS index equality, and all three backpressured segments submitted to TTS. Full fake-only verification: 202 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no cloud/live test or commit. `orchestrator.py` is 247 LOC warning band.

- 2026-09-10 WIB — Sisyphus: Strengthened speaker self-echo veto for real room paths. Instead of one full-candidate correlation, echo detection now searches all realistic positions in bounded recent playback history and scores each delay with the median normalized correlation of multiple 32 ms windows; this tolerates gain, attenuation, and modest room coloration while requiring correlation consistency across candidate speech. A high score vetoes before any `speech_started()` can cancel an active turn. Genuine close-mic speech and double-talk with weaker echo still pass. Accepted/rejected candidate diagnostics now retain echo correlation, best delay ms, candidate RMS, playback-reference RMS, and existing final reason. Added colored delayed echo rejection plus accepted double-talk diagnostic tests. Full fake-only verification: 199 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no live/cloud test or commit. `live_pump.py` is 218 pure LOC.
- 2026-09-10 WIB — Sisyphus: Fixed ambient floor contamination by replacing the active-listening EMA update with a bounded 64-frame median of verified nonspeech RMS only. A frame enters history only when outside active assistant states, below VAD threshold, with no detector speech candidate, no active utterance, and no post-speech silence hangover; user speech and playback-contaminated active frames cannot raise the clean ambient estimate. The resulting value remains frozen for active-turn SNR checks. Added a regression test proving a 0.30 user utterance in LISTENING does not raise a 0.01 ambient floor. Corrected barge-in candidate RMS, ambient RMS, ratio, SNR dB, VAD confidence, and echo correlation output to plain metrics rather than seconds. Full fake-only verification: 198 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no live/cloud test or commit. `live_pump.py` is 218 pure LOC.
- 2026-09-10 WIB — Sisyphus: Fixed active-turn barge-in sensitivity where speaker playback contaminated the ambient EMA and normal close-mic speech was rejected as noise. Ambient RMS now updates only outside active assistant states, preserving the pre-playback floor while TRANSCRIBING/THINKING/SPEAKING. At sustained-candidate evaluation, normalized playback echo correlation now runs before final confidence/SNR rejection: correlated playback rejects as echo, while normal-volume unrelated speech and user speech with weaker echo retain their lower ambient-relative ratio and pass. The SNR guard remains intact. `talk` prints per-candidate RMS, frozen ambient RMS, RMS ratio, SNR dB, VAD confidence, echo correlation, and final reject reason. Fake-only guard covers normal-volume close speech against a frozen ambient floor, alongside existing direct/delayed echo and mixed-speech cases. Full fake-only verification: 197 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no live/cloud test or commit. `live_pump.py` is 242 LOC warning band.
- 2026-09-10 WIB — Sisyphus: Added playback-aware echo rejection after the existing active-turn sustained/VAD/SNR barge-in gate. `SoundDevicePlaybackSink` exposes a passive hook for PCM actually consumed by its callback; `talk` feeds that into a bounded 1 s capture-rate reference. When a candidate reaches sustained acceptance, the gate computes maximum normalized correlation across reasonable speaker-to-mic offsets and rejects correlation >= 0.85 as echo; independent close-mic speech and close-mic speech mixed with weaker echo remain accepted. `talk` reports `barge-in echo correlation` and `barge-in rejected echo`; accepted count remains reported. Fake-only tests prove direct leak rejection, delayed/attenuated echo rejection, unrelated speech acceptance, weaker-echo mixed speech acceptance, and callback PCM hook delivery. Full fake-only verification: 196 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no live/cloud test or commit. `live_pump.py` is 243 LOC warning band.
- 2026-09-10 WIB — Sisyphus: Extended the adaptive interruption gate from SPEAKING to all active assistant states: TRANSCRIBING, THINKING, and SPEAKING. In those states only, raw VAD `SpeechStarted` is withheld until the existing 300 ms sustained, threshold+0.15 confidence, and 3x adaptive RMS-noise-floor checks accept it; rejected candidates do not invoke `orchestrator.speech_started()` and therefore cannot cancel a turn or generate needless provider work. LISTENING continues on the untouched direct VAD dispatch path. Added parameterized fake-only low-noise rejection coverage for TRANSCRIBING/THINKING/SPEAKING, retained sustained close-mic acceptance, and retained LISTENING behavior coverage. Full fake-only verification: 189 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no live/cloud test or commit.
- 2026-09-10 WIB — Sisyphus: Added a speaking-only adaptive barge-in gate without modifying normal LISTENING VAD policy or the existing playback self-trigger guard. The gate calibrates an EMA recent nonspeech RMS floor, requires VAD confidence at least threshold + 0.15, RMS at least 3x that dynamic floor, and approximately 300 ms sustained speech before forwarding a delayed `SpeechStarted`; it rejects weak candidates as noise and unfinished candidates on `SpeechEnded` as short. `LiveDiagnostics`/`talk` now report `barge_in_accepted`, `barge_in_rejected_noise`, and `barge_in_rejected_short`. Fake-only tests prove low-level speaking noise rejects, short close-mic transient rejects, sustained close-mic speech accepts, and low-level speech in normal LISTENING keeps existing VAD behavior. Full fake-only verification: 187 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no live/cloud test or commit.
- 2026-09-10 WIB — Sisyphus: Fixed Phase 6 TTS TaskGroup recovery. `SpeechPipeline` now recursively flattens task-group exception leaves: a singleton leaf remains unwrapped for existing provider recovery; exactly one `TtsError` plus any `anyio.BrokenResourceError` cancellation sibling is unwrapped to that `TtsError`; any other sibling remains an `ExceptionGroup` and fatal. This lets the existing orchestrator TTS provider-error path stop playback, preserve safe diagnostics, transition ERROR -> IDLE, and accept later speech without stale pipeline output. ElevenLabs `empty_audio` is raised only after terminal websocket processing when zero decoded nonempty PCM chunks were yielded; `is_final_audio_for_turn` and `is_final` control frames are already valid/handled, but control-only output remains a recoverable malformed-provider-audio result rather than being ignored. Added fake-only guards for nested TTS groups, TTS plus BrokenResource sibling, unexpected groups, and control-only ElevenLabs frames. Full fake-only verification: 183 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no live/cloud test or commit.
- 2026-09-10 WIB — Sisyphus: Investigated Gemini first-token latency with no Gemini/prompt/model/VAD/STT/TTS/playback change and no cloud test. `talk` constructs one `OpenAiCompatibleProvider` and one `httpx.AsyncClient` at startup; every turn calls `stream()` on that same provider/client, and `RuntimeOrchestrator.aclose()` closes it only at runtime shutdown. Client pooling is active via `httpx.AsyncHTTPTransport(http2=True)` with 200 max connections, 40 keep-alive connections, and 30 s expiry. Existing per-turn `talk` diagnostics already expose request-to-first raw SSE delta and request-to-first usable delta; provider terminal metrics also retain request-to-response-header TTFB (`http_headers_seconds`) and first SSE timing. Therefore a clean 9.304 s request-to-usable delta is upstream/network/provider latency after orchestration, not a client/session recreation or runtime buffer. Offline verification: 179 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no commit.
- 2026-09-10 WIB — Sisyphus: Removed first-turn model-load cost from the Phase 6 live path without changing VAD, model selection, TTS, or playback. `STTProvider` now exposes synchronous `preload() -> float`; `FasterWhisperSttProvider.preload()` loads/reuses the same `_model` instance and `transcribe()` times only model inference plus lazy segment consumption. `talk` runs preload in an AnyIO worker before printing `STATE: LISTENING` and emits `STT model load: … s`; the first STT invocation therefore measures inference rather than model construction. Fake-loader regression test proves separated 2.0 s preload / 4.0 s inference and one loader call. Full fake-only verification: 179 pytest passed, compileall/lock/diff checks and CLI help passed; LSP daemon unavailable; no live mic/speaker/provider call or commit.
- 2026-09-10 WIB — Sisyphus: Phase 6 local STT latency attribution completed without a cloud/device call. `talk` and `stt-wav` both construct the same `SttConfig` defaults/flags (CPU INT8 `large-v3-turbo`, `cpu_threads=0`, `num_workers=1`); `talk` constructs one provider at startup and `FasterWhisperSttProvider` retains `_model`, with fake-loader coverage proving one load over multiple turns. Cache-only serial matrix used `recordings/live-utterances/turn-0002.wav` (1.792 s, Indonesian transcript `Halo, kamu bisa dengar aku?`): large-v3-turbo loads in 1.593/1.859/1.933 s for 4/6/8 threads; five warm inferences median 4.951/3.823/3.946 s respectively. Recommended next live test: retain `large-v3-turbo`, set `--cpu-threads 6 --num-workers 1`, and preserve language accuracy validation; `small` is absent from the local cache, so all three requested small cells are blocked rather than downloaded. Cache-only `stt-wav` at 6 threads returned 3.184 s/RTF 1.777 with the same transcript. Added runtime LLM timing fields/diagnostics: invocation start, provider request start, first raw delta (provider first SSE), and first usable delta (first text chunk), with a fake-only raw-vs-usable regression test. Full fake-only verification: 178 pytest passed, compileall/lock/diff checks and CLI help passed; LSP daemon unavailable; no live mic/speaker/provider call or commit.
- 2026-09-10 WIB — Sisyphus: Fixed Phase 6 interruption ownership and STT admission cancellation. A turn-local AnyIO `CancelScope` suppresses its own `CancelledError`, which previously allowed interrupted turns to be finalized successfully; the orchestrator now checks `scope.cancelled_caught`/owned cancellation after scope exit and records `CANCELLED` results with the `InterruptionDiagnostic`. STT admission keeps concurrency at one, distinguishes pre-start cancellation from a completed stale inference result, and stale queued turns do not invoke STT after supersession. Extracted speech streaming and STT-admission diagnostics into focused modules; `orchestrator.py` is 245 LOC. Updated live-pump fakes for the indexed speech-start frame parameter. Full fake-only verification: 177 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no live mic/speaker/provider call or commit.
- 2026-09-09 WIB — Sisyphus: Tightened recoverable provider diagnostics and playback metrics. Turn errors retain allowlisted static provider, exception class, enum kind/message, HTTP status/code, and recoverable flag only; no raw exception text, headers, payloads, or secrets. Recoverable failures still detach/error->idle and providers close only at runtime shutdown. Playback diagnostics split `playback_starvations` (silent callback while active pipeline expects more audio) from backend-confirmed `device_output_underflows`; idle/stop/final-padding silence is neither. Full fake-only verification: 176 pytest passed, compileall/lock/diff checks passed; no live call or commit.
- 2026-09-09 WIB — Sisyphus: Fixed recoverable provider turn failures leaving stale active ownership in ERROR. Current STT/LLM/TTS provider errors now detach the failed turn, stop playback, preserve a staged `TurnResult`, transition ERROR -> IDLE, and let the next speech start create fresh IDs legally. Unexpected/internal and device failures remain fatal. Live diagnostics now print `turn error: provider (stt|llm|tts)`. Playback underruns now count only backend `output_underflow`, not intentional idle/stop silence or finite chunk drain; producer saturation remains `backpressure_waits`. Fake-only verification: 174 pytest passed, compileall/lock/diff checks passed; no live call or commit.
- 2026-09-09 WIB — Sisyphus: Confirmed live utterance corruption root cause: `audio_frame_from_samples()` used `np.ascontiguousarray()`, which preserved a view of reusable PortAudio float32 callback memory. Raw pump dump could write a frame before reuse while collector-held frame references were overwritten by later callbacks, yielding noise-only utterances. Replaced it with an owning C-contiguous float32 copy. Regression tests mutate source callback buffers after submission, verify no shared memory, verify collector/Utterance samples and order persist after completion/reset/additional frames, and verify canonical raw pre-VAD dump content. Full offline verification: 172 pytest passed; compileall/lock/diff checks passed; no live call or commit.
- 2026-09-09 WIB — Sisyphus: Added diagnostic-only `talk --dump-raw-capture PATH` before VAD/collector plus startup requested/resolved-device and canonical 16 kHz mono float32/512-frame diagnostics. Source comparison proved `record` and `talk` both resolve named devices per run and request the same canonical format, but `talk` uses callback+internal Queue+AnyIO pump stages that `record` bypasses; no single source-level root cause is claimed. Opt-in utterance dumps remain pre-STT. Runtime reports exact STT-input sample count/peak/RMS/VAD frames and STT wait/invocation/inference/total. Faster-Whisper now lazy-loads once per provider instance. Fake-only verification: 170 pytest passed; compileall/lock/diff/talk help passed; no live call or commit.
- 2026-09-09 WIB — Sisyphus: Added opt-in pre-STT utterance instrumentation without a live run. `talk --dump-utterances-dir` writes `turn-0001.wav`-style canonical 16 kHz mono PCM16 WAV artifacts immediately before the same Utterance enters STT; default remains no retention. Runtime now distinguishes STT wait, invocation wall time, provider inference time, and total. `FasterWhisperSttProvider` now lazy-loads once and reuses the model per provider instance; no transcription arguments/model selection changed. Fake-only verification: 169 pytest passed, compileall/lock/diff checks passed.
- 2026-09-09 WIB — Sisyphus: Fixed first live-test capture starvation and playback full-buffer failure without a new live call. `CaptureVadPump` now keeps capture/VAD consumption in its task group and submits completed utterance processing as owned child tasks, so assistant STT/LLM/TTS no longer blocks speech-start detection. `SoundDevicePlaybackSink` has bounded condition-backed producer backpressure instead of `AudioCliError("playback buffer full")`; callback remains nonblocking, drains/silences on real underrun, and stop/aclose invalidate+wake waiting writers to prevent stale resume. Live diagnostics report capture overruns, playback underruns, playback backpressure waits, self-VAD suppressions, and cancelled-turn interruptions. Fixed an abandoned capture worker shutdown hang. Full fake-only verification: 166 pytest passed, compileall/lock/diff checks passed; no live call or commit.
- 2026-09-09 WIB — Sisyphus: Phase 6 Slice 3 controlled live path implemented but not live-tested. Added bounded `CaptureVadPump` reusing existing mic/VAD/collector contracts, real incremental `SoundDevicePlaybackSink`, playback-consumption acknowledgement plus 300 ms VAD self-trigger guard, and `holo-companion talk` scaffolding with concise safe diagnostics. Fake-only live-pump/playback tests pass. Fixed an abandoned capture worker hang by adding pump stop signalling and breaking when the event loop is unavailable. Verified full `pytest` 163 passed; compileall, lock check, diff check, and `talk --help` passed. No live mic/speaker/provider call, no commit, no Slice 4.
- 2026-09-09 WIB — Sisyphus-Junior: Phase 6 Slice 2 implemented on top of accepted Slice 1. Added pure `IncrementalSpeechSegmenter` with `.?!…`/newline boundaries, residual flush, whitespace ignore, and 160-char whitespace fallback; added bounded AnyIO `SpeechPipeline` with text/audio capacity 2, ordered text->TTS->audio->sink stages, natural backpressure, cancellation-safe queues, stale guards before TTS and before/after playback, style/sequence work items; integrated orchestrator so `SPEAKING` starts on first emitted segment and first timestamps remain first-only. Verified `uv run --locked pytest` 155 passed, compileall clean, `uv lock --check` clean. LSP diagnostics unavailable because daemon socket timed out. `orchestrator.py` is 247 pure LOC, below but near the 250 ceiling.
- 2026-09-08 WIB — Sisyphus-Junior: Phase 6 is IN PROGRESS with the first provider-neutral runtime slice. Added AnyIO `RuntimeOrchestrator`, exact `RuntimeState` table, typed invalid transition errors, monotonic turn/generation IDs, serialized non-abandoning STT thread adapter, cancellation-handle plus task-scope interruption for LLM/TTS, stale guard before playback admission, tiny async playback protocol, nullable timestamp metrics, and fake-driven tests. No mic/VAD pump, hardware playback driver, provider internals, memory, RVC, avatar, or live API calls. `orchestrator.py` is 246 pure LOC warning band; split before adding substantial logic.
