# Named Sub-Index: `llm`

## Entries

- 2026-09-10 WIB — Sisyphus: Added opt-in exact request diagnostics via `LLM_DEBUG_REQUEST=1` to the generic OpenAI-compatible adapter. Immediately before `client.stream()`, it prints one safe JSON line derived from the exact `json=` body: resolved base URL/model, message count, roles, complete system/user/history message contents, and all generation fields actually sent (`stream: true`; temperature/top_p/max_tokens/max_completion_tokens/stop are null because they are not sent). API key and Authorization headers are never included. Inspection confirms `talk` creates the normal `ConversationContext` system prompt plus current user message for a first turn; no developer prompt, hidden concise instruction, or additional generation parameter exists. Regression test proves debug payload matches sent body and excludes secret. Full offline verification: 210 pytest passed, compileall/lock/diff checks passed; no live/cloud test or commit.
- 2026-09-10 WIB — Sisyphus: Hardened generic OpenAI-compatible SSE parsing for observed 9Router stream shapes. Empty `choices: []` events now ignore safely; role-only/empty deltas remain non-text; `delta.content: null` plus a non-null `finish_reason` completes normally; duplicate `[DONE]` is consumed idempotently. Malformed JSON, non-object payloads, missing/non-list choices, non-object choices/deltas, and non-string non-null content remain typed protocol errors. Runtime LLM provider diagnostics now identify `openai-compatible` rather than Gemini-specific naming. Added exact observed sequence regression: empty choices, role-only, text chunks, null-content finish with usage, DONE, duplicate DONE. Full offline verification: 209 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no live/cloud test or commit.
- 2026-09-10 WIB — Sisyphus: Refined the provider-independent Holo persona prompt for flowing spoken conversation. It now defaults to one short paragraph with 2-3 complete natural Indonesian sentences, bans standalone telegraphic fragments (`Mudah.`, `Cepat.`, `Enak.`) and note-like labels (`Opsi lain:`), encourages natural conjunctions (`kalau`, `terus`, `apalagi`, `jadi`, `tapi`, `atau`), and includes a natural nasi-goreng response style example. Existing personality, anti-helpdesk, Indonesian-language, Markdown, no-human-claim, and explicit-detail-request rules remain. Context tests still assert structural prompt delivery rather than pinned prose. Full offline verification: 208 pytest passed, compileall/lock/diff checks passed; no live/cloud test or commit.
- 2026-09-10 WIB — Sisyphus: Refined Holo's conversational companion persona in `src/holo_companion/llm/context.py` (`PERSONA_PROMPT`) without touching STT, VAD, TTS, model, or provider architecture. Prompt explicitly defines a warm, casual young Indonesian companion (not a helpdesk/robot), prioritizing 1-3 natural spoken sentences, direct answers first, at most one follow-up question when relevant, natural fillers ('hmm', 'iya', 'kayaknya'), banning canned robotic phrases ('Tentu', 'Berikut adalah', 'Rekomendasi umum'), preventing question repetition/surveys/over-explanation, never claiming to be human, and retaining conversational Indonesian with Markdown/lists prohibited unless explicitly requested. Added prompt structure regression tests. Full offline verification: 208 pytest passed, compileall, uv lock --check, git diff --check passed.
- 2026-09-10 WIB — Sisyphus: Switched the configurable default OpenAI-compatible runtime endpoint to local 9Router `http://localhost:20128/v1` and model `ag/gemini-3.6-flash-low`. The streaming provider, transport, cancellation handle, turn ownership, and runtime timing instrumentation are unchanged. `LLM_API_KEY` remains environment-only; new optional `LLM_BASE_URL` and existing `LLM_MODEL` environment overrides take precedence over `[llm]` config and are documented without credentials. Fake-only config tests prove endpoint/model override precedence; no request to 9Router or any cloud provider was made. Full offline verification: 191 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no commit.

- 2026-09-10 WIB — Sisyphus: Switched the configured Gemini development default from `gemini-3.6-flash` to `gemini-3.5-flash` in `config/default.toml`, with no provider, prompt, STT, VAD, TTS, playback, or transport change. `load_llm_config()` now allows an optional `LLM_MODEL` environment override, which takes precedence over `[llm].model`; `.env.example` documents it and a fake-only regression test proves precedence. Full offline verification: 190 pytest passed, compileall/lock/diff checks and `talk --help` passed; LSP daemon unavailable; no live/cloud test or commit.
- 2026-08-27 WIB — Sisyphus: Phase 4 accepted with Gemini as the current development provider. Live metrics: headers 3.727083404 s, first SSE 3.727319433 s, first visible token 3.727335013 s, total 3.728454544 s, 2 chunks; async cleanup is clean. The close alignment proves adapter parsing does not buffer; the pre-header delay is Gemini/upstream latency, retained as a later performance concern rather than a correctness blocker. Phase 5 is `NEXT`; no TTS code/model was started.
- 2026-08-27 WIB — Sisyphus-Junior: Added narrow Phase 4 LLM latency attribution metrics to the current uncommitted stable-`httpx` OpenAI-compatible stream. `GenerationMetrics` now carries request-relative nullable `http_headers_seconds` and `first_sse_event_seconds` alongside TTFT, total, and chunk count; headers are timestamped immediately after entering the response context, first raw SSE before `[DONE]`/JSON/content handling, TTFT only on first nonempty content delta, and total after context exit. CLI terminal NDJSON includes the new metric keys through existing `asdict(result.metrics)`. Mock-transport tests prove role-only first SSE is measured separately and visible chunks are yielded before terminal completion. No real API call, dependency, config/persona, roadmap, Phase 5, or commit. Verification: focused `uv run pytest tests/test_openai_compatible_provider.py tests/test_llm_cli.py` 13 passed; full `uv run pytest` 91 passed; `uv run python -m compileall src tests` passed; `uv lock --check` passed; `uv run llm-test --help` passed; LOC all touched files under 200 pure LOC. LSP daemon still unreachable at `/home/arjuna/.omo/lsp-daemon/v0.1.0/daemon.sock`. Phase 4 remains `NEXT`; Phase 5 remains blocked.
- 2026-08-27 WIB — Sisyphus: Fixed provider-independent async shutdown failure observed after successful Gemini and OpenRouter streams. Root cause was `httpx2 2.12.0` / `httpcore2 2.12.0` `PoolByteStream.__aiter__` lifecycle (`GeneratorExit`, `generator didn't stop after athrow()`), compounded by `llm-test` not owning client shutdown. Replaced adapter transport with stable `httpx 0.28.1` / `httpcore 1.0.9`; `httpx2/httpcore2` are removed. `llm-test` explicitly closes the outer stream generator and provider client in `finally`. Full suite passed 89 tests. Live Gemini retry is pending; Phase 5 remains blocked.
- 2026-08-27 WIB — Sisyphus-Junior: Corrected Phase 4 LLM stream contracts in uncommitted implementation. Replaced frozen boolean `CancellationToken` behavior with mutable `CancellationHandle.cancel()` / `is_cancelled()` while preserving `CancellationToken` as a compatibility alias. OpenAI-compatible SSE now returns terminal `GenerationResult(status=cancelled)` with accumulated text after post-start cancellation and closes the response context; pre-cancel still raises `LlmError(CANCELLED)` before transport. EOF without `[DONE]` or non-null choice `finish_reason` now raises `LlmError(PROTOCOL)`; non-null `finish_reason` completes even without `[DONE]`; empty role deltas remain non-chunks. CLI result NDJSON now emits typed `status` instead of raw payload dumps. Regression tests added under `tests/test_openai_compatible_provider.py`, `tests/test_llm_cli.py`, and `tests/test_llm_base_config_context.py`; no real HTTP/API request, secrets, model download, roadmap change, commit, or Phase 5 work. Verification: targeted pytest 23 passed; full `uv run pytest` 89 passed; `compileall` passed; `uv lock --check` passed; `llm-test --help` passed; root parser help verified via `build_parser().print_help()`. LSP diagnostics could not complete because `/home/arjuna/.omo/lsp-daemon/v0.1.0/daemon.sock` stayed unreachable/timed out. Phase 4 remains `NEXT`; Phase 5 remains blocked.
- 2026-08-27 WIB — Sisyphus-Junior: Phase 4 automated LLM streaming slice implemented and left uncommitted. Added direct `httpx2[brotli,http2,zstd]` OpenAI-compatible SSE provider under `src/holo_companion/llm/`, typed config/context/cancellation/chunk/final-result/metrics, concise Indonesian persona, and in-memory context trimming only. Added separate `llm-test --prompt` package script that emits flushed NDJSON chunk lines then terminal result; config reads nonsecret `config/default.toml` blanks plus `LLM_API_KEY` from environment only and does not auto-load `.env`. Tests use `httpx2.MockTransport` and custom async streams; no real LLM/API request was made. Verification passed: `uv sync --locked`, `uv lock --check`, full `pytest` 86 passed, `compileall`, `llm-test --help`, root CLI help, `uv tree`, LOC ceiling. LSP daemon stayed unreachable after retry, so LSP diagnostics could not complete. ROADMAP not updated; Phase 4 remains `NEXT` until paid API handoff/manual feel check.
